新建工作空间
mkdir -p ~/workspace-name/src
cd ~/workspace-name/src 
catkin_init_workspace
cd ~/workspace-name/
catkin_make

新建包(后面跟着的是依赖)
包的名字只能是小写字母开头，内容只允许小写字母、下划线、数字
catkin_create_pkg listen rospy roscpp std_msgs geometry_msgs
